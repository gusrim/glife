# daystart

daystart = (year - 2015) * 365
daystart += ((year - 2015) + 3) / 4

i=1
if month > i:
	:daystart

	daystart += monthsend[i]
	i += 1

	loop'daystart'
end

daystart += day

daystart -= daystart[0]

--- daystart ---------------------------------

