# daystart

daystart = (year - 2015) * 365
daystart += ((year - 2015) + 3) / 4

i=1
:daystart

if month > i:

	daystart += monthsend[i]
	i += 1

	jump 'daystart'
end

daystart += day

daystart -= daystart_start

daystart += 1

--- daystart ---------------------------------

